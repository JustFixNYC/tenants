var createUserOnboard = function(req, res) {
	// Take all user information given during onboarding, submit the data in the correct order
}

var updateUser = function(req, res) {
	// should update user with new data, assuming the user has submitted the new data correctly (return errors as they should)
}

var resetPassword = function(req, res) {
	// Reset user password after authorization has been confirmed
}