<section class="onboarding onboarding-code-entr">

	<div ng-show="referralSuccess == false">
		<h3>Welcome to<br/>JustFix.nyc!</h3>
		<p>Please enter the access code provided by your advocate so you can gain access to the website</p>
		<div class="error-text alert alert-danger" ng-show="majorError">Whoa, looks like we're having some trouble on our end. Please refresh this page and try again.</div>
		<form>
		  <div class="form-group">
		    <label class="control-label">Access Code</label>
		    <input class="form-control" type="text" placeholder="Starts with..." ng-model="newUser.accessCode" class="input-code">
		  </div>
		  <div class="form-group">
		    <input class="btn btn-primary input-submit" value="Get Started!" ng-click="referralSuccess = true"><!-- validateCode() -->
		  </div>
		</form>
	</div>
	<div class="error-text alert alert-warning" ng-show="codeError">Uh oh! There was an error with this code. Please double check the code entered is correct and try again.</div>
	<div ng-show="referralSuccess == true">
		<h3>Your Building Advocate</h3>
		<p>Based on the access code you provided, we've connected you to the advocate working with your building. Their contact information will be stored in your account, so you can cannect with them at any time.</p>
		<div class="referral">
			<p class="name">{{referral.name}}</p>
			<p class="org">{{referral.organization}}</p>
			<p class="phone">{{referral.phone}}</p>
			<p class="email">{{referral.email}}</p>
		</div>
		<div class="button-wrapper">
			<span class="btn btn-default pull-left" ng-click="referralSuccess = false">Back</span>
			<span class="btn btn-primary pull-right" ng-click="goNext()">Continue</span>
		</div>
	</div>
</section>
